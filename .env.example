ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_PLACES_API_KEY=AIza...

KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=us2

# Auto-injected once you connect the Postgres (Neon) integration in the
# Vercel dashboard: Storage -> Connect Database -> Postgres. Run
# `vercel env pull` locally to fetch the real values.
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=

# Google Cloud Console -> APIs & Services -> Credentials -> OAuth client ID
# (Web application). Authorized redirect URI: <your domain>/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
